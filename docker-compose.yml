services:
  postgres:
    image: postgres:16-alpine
    container_name: assetz-db
    environment:
      POSTGRES_USER: assetz
      POSTGRES_PASSWORD: assetz_dev
      POSTGRES_DB: assetz
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
